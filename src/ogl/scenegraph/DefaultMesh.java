package ogl.scenegraph;

public interface DefaultMesh {
	 public abstract void render (int vertexAttribIdx, int colorAttribIdx, int normalAttribIdx, int textureAttribIdx);

	    public abstract void render();
}
